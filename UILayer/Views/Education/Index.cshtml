@model List<EducationListDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
    int number = 0;
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Inverse table</h4>
            <p class="card-description">
                Eğitim
            </p>
            <div class="table-responsive">
                <table class="table table-dark">
                    <thead>
                        <tr>
                            <th> # </th>
                            <th> Oku Adı </th>
                            <th> Bölüm Adı </th>
                            <th> Başlangıç </th>
                            <th> Bitiş </th>
                            <th>  </th>
                            <th>  </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            number += 1;
                            <tr>
                                <td> @number </td>
                                <td> @item.Name </td>
                                <td> @item.Major </td>
                                <td> @item.StartingDate </td>
                                <td> @item.EndDate </td>
                                <td class="action-buttons">
                                    <a href="/Education/UpdateEducation/@item.Id" class="btn btn-inverse-success btn-fw">Güncelle</a>
                                </td>
                                <td class="action-buttons">
                                    <a href="/Education/DeleteEducation/@item.Id" class="btn btn-inverse-danger btn-fw">Sil</a>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
                <br />
                <a href="/Education/AddEducation/" class="btn btn-inverse-success btn-fw">Okul Ekle</a>
            </div>
        </div>
    </div>
</div>